# Story 1.10 — Correção de Crashes Críticos e Bugs de UX

**Épico:** EP-01 Fundamentos Geométricos / Estabilidade
**Sprint:** HOTFIX
**Status:** Done
**Agentes:** @dev (Dex)
**Data:** 2026-03-17

---

## User Story
Como usuário do Aura-6.1,
quero que o sistema não apresente crashes de TypeScript nem comportamentos incorretos de UX,
para que o build e a experiência de uso sejam estáveis.

## Acceptance Criteria

- [x] AC-1 (UX4): `projecaoFinanceira` não tem declaração duplicada — build limpo (havia 1 declaração, erro não confirmado)
- [x] AC-2 (UX5): `modeloCurva` — não existe no codebase; UX5 não se aplica (resolvido por não existência)
- [x] AC-3 (UX7): `dataInicio` importado corretamente em `orcamento/page.tsx` via `useProject()` — já estava correto
- [x] AC-4 (M7): CDT com `custosTarefas` vazio usa fallback proporcional (orcamentoBase / nTarefas) em vez de crashar
- [x] AC-5 (UX11): Sem marcadores de debug `border-red` ou `EAP-V3` encontrados no codebase
- [x] AC-6 (UX13): Botão "Limpar Tudo" adicionado na EAP com `window.confirm()` antes de executar
- [x] AC-7 (B3): Banner EAP persiste estado "dispensado" em `localStorage` — não reaparece após fechar
- [x] AC-8 (B5): `bigdig.test.ts` (deprecated, API v1) removido; mantido `big-dig-simulation.test.ts` (API v2, completo)
- [x] AC-9: 3 erros TypeScript `cet_dupla missing` corrigidos nos mocks de `CDTResult`

## Tasks

- [x] 1. Investigar UX4 — `projecaoFinanceira` duplicado (não confirmado, único useMemo em orcamento/page.tsx)
- [x] 2. Investigar UX5 — `modeloCurva` não existe no codebase (já resolvido ou nunca existiu)
- [x] 3. Investigar UX7 — `dataInicio` já importado via `useProject()` na linha 16 de orcamento/page.tsx
- [x] 4. Corrigir M7 — fallback em `curvaCusto` quando `custosTarefas` vazio (CDT page)
- [x] 5. Verificar UX11 — sem debug markers visuais no codebase
- [x] 6. Corrigir UX13 — adicionar `window.confirm()` em `handleClearAll` + expor botão "Limpar Tudo" na hierarquia EAP
- [x] 7. Corrigir B3 — persistir `aura_eap_banner_dismissed_${projetoId}` no localStorage ao dispensar banner; limpar flag ao exportar nova EAP
- [x] 8. Corrigir B5 — remover `src/lib/engine/bigdig.test.ts` (usa API v1 deprecated)
- [x] 9. Corrigir TS — adicionar `cet_dupla: { valid: true }` nos 3 mocks CDTResult (page.tsx, report/page.tsx, gerenciamento/page.tsx)

## File List

- `src/app/(dashboard)/[projetoId]/motor/cdt/page.tsx` — M7: fallback custosTarefas
- `src/app/(dashboard)/[projetoId]/setup/eap/page.tsx` — UX13: confirm + botão Limpar Tudo; B3: clear dismissed flag on export
- `src/app/(dashboard)/[projetoId]/setup/cpm/page.tsx` — B3: persist dismissed flag; restore on new export
- `src/app/(dashboard)/[projetoId]/page.tsx` — TS: add cet_dupla to mock CDTResult
- `src/app/(dashboard)/[projetoId]/report/page.tsx` — TS: add cet_dupla to mock CDTResult
- `src/app/(dashboard)/[projetoId]/governanca/gerenciamento/page.tsx` — TS: add cet_dupla to mock CDTResult
- `src/lib/engine/bigdig.test.ts` — B5: REMOVIDO (deprecated)

## Definition of Done

- [x] TypeCheck: 0 erros (era 3 antes)
- [x] Vitest: 242 testes passando em 13 arquivos (removido bigdig.test.ts com 2 testes deprecated)
- [x] Bugs críticos corrigidos: M7, UX13, B3, B5, TS cet_dupla

---

## Quality Gates

- @dev: todos os ACs verificados manualmente no codebase (UX4/5/7 não se aplicavam — resolvidos por não existência)
- @qa: 242 testes passando após hotfix, 0 regressões
- TypeCheck: 0 erros novos

---

## QA Results
```yaml
storyId: 1.10
verdict: PASS
issues: []
```
