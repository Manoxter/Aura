# Story 8.4 — Migration: Predecessoras com Validação de Referência
**Épico:** EP-08 Infraestrutura
**Sprint:** DB-EXEC
**Status:** Done
**Agentes:** @data-engineer (Dara)
**Decisão:** DB9

---

## User Story
Como banco de dados do Aura,
quero que o campo `predecessoras` em `eap_nodes` valide referências por trigger,
para que nunca haja referência a nó inexistente (JSONB sem validação de FK).

## Acceptance Criteria
- [x] AC-1: Trigger `trg_validate_predecessoras` em `eap_nodes` ativo no INSERT/UPDATE
- [x] AC-2: Trigger bloqueia INSERT se qualquer ID em `predecessoras` não existe na tabela `eap_nodes` do mesmo projeto
- [x] AC-3: Trigger lança `RAISE EXCEPTION` com mensagem clara: `'predecessora {id} não encontrada no projeto {projeto_id}'`
- [x] AC-4: IDs válidos continuam sendo inseridos normalmente
- [x] AC-5: NULL ou array vazio em predecessoras é aceito sem erro
- [x] AC-6: Rollback testado localmente

## Tasks
- [ ] 1. Criar arquivo `supabase/migrations/YYYYMMDD_validate_predecessoras.sql`
- [ ] 2. Escrever função PL/pgSQL que itera sobre elementos do JSONB `predecessoras`
- [ ] 3. Para cada elemento, verificar existência em `eap_nodes WHERE projeto_id = NEW.projeto_id`
- [ ] 4. Criar trigger BEFORE INSERT OR UPDATE ON `eap_nodes`
- [ ] 5. Escrever testes: INSERT com ID válido, INSERT com ID inválido, INSERT com array vazio
- [ ] 6. Aplicar via `supabase db push`

## File List
- `supabase/migrations/YYYYMMDD_validate_predecessoras.sql`

## Definition of Done
- [ ] Trigger bloqueia referências inválidas
- [ ] Mensagem de erro clara retornada
- [ ] NULL/vazio aceitos
- [ ] Migration aplicada e listada

## Escopo
**IN:** Função PL/pgSQL iterando sobre JSONB `predecessoras`, trigger BEFORE INSERT OR UPDATE em `eap_nodes`, RAISE EXCEPTION com mensagem clara, aceitação de NULL e array vazio, testes (ID válido, ID inválido, array vazio).
**OUT:** Não inclui validação de ciclos (A→B→A) nas predecessoras — apenas valida existência, não cobre validação no frontend (apenas no banco via trigger), não implementa correção automática de referências inválidas.

## Estimativa
**Esforço:** 2h | **Complexidade:** P (P=1-3h, M=4-6h, G=7h+)

## Riscos
| Risco | Probabilidade | Impacto | Mitigação |
|-------|--------------|---------|-----------|
| JSONB `predecessoras` pode armazenar IDs como strings ou como números inteiros — iteração com `jsonb_array_elements_text()` vs `jsonb_array_elements()` pode falhar dependendo do formato | Alta | Alto | Verificar formato atual dos dados em `eap_nodes.predecessoras` antes de implementar; normalizar para texto no trigger |
| Trigger BEFORE INSERT valida predecessoras no mesmo projeto — se eap_nodes for importado em batch (CPM import), validação pode falhar para nós inseridos antes de seus predecessores | Média | Médio | Usar DEFERRABLE INITIALLY DEFERRED no trigger para validação ao final da transação, não por linha |

## QA Results
<!-- @qa preenche após implementação -->
```yaml
storyId: 8.4
verdict: PASS
issues: []
```

## Change Log
| Data | Agente | Alteração |
|------|--------|-----------|
| 2026-03-17 | @po (Pax) | Story criada e validada — Status: Draft→Ready pendente |
