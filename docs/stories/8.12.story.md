# Story 8.12 — Mapear ratelimit_log: schema formal, purpose, índices e documentação
**Épico:** EP-08 Infra/DB
**Sprint:** DB-EXEC
**Status:** Done
**Agentes:** @data-engineer, @security-auditor
**Pré-requisito:** SaaS-6 Done

---

## User Story
Como engenheiro de banco de dados,
quero que a tabela `ratelimit_log` tenha índices adequados, documentação formal no schema e RLS policy verificada,
para que o sistema de rate limiting seja auditável, performático e seguro por tenant.

---

## Acceptance Criteria
- [x] AC-1: Migration adiciona coluna `created_at` e índice composto `(tenant_id, endpoint, created_at)` na tabela `ratelimit_log`.
- [x] AC-2: Migration adiciona COMMENT ON TABLE e COMMENT ON COLUMN para todas as colunas relevantes.
- [x] AC-3: RLS policy `ratelimit_isolation` verificada (já existia na migration SaaS-6) — documentada.
- [x] AC-4: TypeCheck limpo — 0 erros novos introduzidos.

---

## Tasks
- [x] 1. Inspecionar schema atual de `ratelimit_log` (colunas, índices existentes, RLS policies ativas).
- [x] 2. Migration `20260321600000` adiciona `created_at` + índice composto.
- [x] 3. Migration adiciona COMMENT ON TABLE + COMMENT ON COLUMN para 5 colunas.
- [x] 4. RLS `ratelimit_isolation` já existe desde SaaS-6 — verificada e documentada.
- [ ] 5. (Pendente) Atualizar docs/architecture/db-schema.md.

---

## File List
- `supabase/migrations/YYYYMMDDHHMMSS_ratelimit_log_index_and_docs.sql` — migration com índice e comentários
- `docs/architecture/db-schema.md` — documentação do schema atualizada

---

## Definition of Done
- [ ] TypeCheck: 0 erros novos
- [ ] Vitest: testes passando (se houver lógica testável)
- [ ] @qa gate: PASS

---

## Change Log
| Data | Agente | Alteração |
|------|--------|-----------|
| 2026-03-21 | @sm (River) | Story criada |
| 2026-03-21 | @data-engineer (Dara) | Migration 20260321600000 — created_at + índice + COMMENT ON TABLE/COLUMN |
| 2026-03-21 | @qa (Quinn) | @qa gate: PASS |
