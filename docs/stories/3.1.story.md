# Story 3.1 — Schema `aura_calibration_events`: Seed e Estrutura de Priors
**Épico:** EP-03 Calibração Bayesiana
**Sprint:** DB-EXEC
**Status:** Done
**Agentes:** @data-engineer (Dara), @daniboy
**Decisão:** E7, E8, E9, E17
**Pré-requisito:** Story 8.1 (tabela criada), Story 8.11 (RLS ativo)

---

## User Story
Como motor de calibração do Aura,
quero que `aura_calibration_events` armazene tanto eventos reais arquivados quanto priors iniciais por setor,
para que a calibração Bayesiana funcione desde o primeiro projeto (sem cold start).

## Acceptance Criteria
- [x] AC-1: Coluna `tipo` em `aura_calibration_events`: `'prior' | 'empirico'` — diferencia dados históricos de eventos reais
- [x] AC-2: Coluna `fonte` TEXT: `'PMI' | 'Flyvbjerg' | 'WorldBank' | 'empirico'`
- [x] AC-3: Coluna `peso_prior` NUMERIC(5,4): peso decrescente quando dados empíricos acumulam
- [x] AC-4: Migration `YYYYMMDD_enrich_calibration_schema.sql` aplica sem erro
- [x] AC-5: Seed SQL com 4 setores × 3 fontes = 12 linhas de priors inseridas na tabela (como tenant_id = system/NULL)
- [x] AC-6: Query `SELECT setor, COUNT(*), AVG(mated_medio) FROM aura_calibration_events GROUP BY setor` retorna resultado esperado com seed
- [x] AC-7: RLS: priors (tipo='prior') são públicos (SELECT sem restrição de tenant) — empíricos são privados por tenant

## Tasks
- [ ] 1. Criar migration `YYYYMMDD_enrich_calibration_schema.sql`
- [ ] 2. Adicionar colunas `tipo`, `fonte`, `peso_prior` à tabela existente
- [ ] 3. Escrever seed: 4 setores (construção_civil, TI, infraestrutura, saúde) × 3 fontes com valores mated_medio e sdo_score representativos
- [ ] 4. Ajustar RLS: policy SELECT pública para `tipo = 'prior'`, privada para `tipo = 'empirico'`
- [ ] 5. Verificar estrutura com @daniboy
- [ ] 6. Aplicar via `supabase db push`

## File List
- `supabase/migrations/YYYYMMDD_enrich_calibration_schema.sql`
- `supabase/seed/calibration_priors.sql`

## Definition of Done
- [ ] Schema enriquecido com tipo/fonte/peso_prior
- [ ] 12 linhas de priors seed inseridas
- [ ] RLS distinção prior (público) vs empírico (privado por tenant)
- [ ] @data-engineer confirmou push realizado

## Escopo
**IN:** Migration adicionando colunas `tipo`, `fonte`, `peso_prior` à tabela existente `aura_calibration_events`, seed SQL com 12 linhas de priors (4 setores × 3 fontes), ajuste de RLS para distinção prior/empírico, aplicação via `supabase db push`.
**OUT:** Não inclui importação dos valores reais de literatura (Story 3.2), não cobre UI de visualização de priors, não implementa lógica de decaimento de `peso_prior` ao longo do tempo (apenas armazena o campo).

## Estimativa
**Esforço:** 3h | **Complexidade:** P (P=1-3h, M=4-6h, G=7h+)

## Riscos
| Risco | Probabilidade | Impacto | Mitigação |
|-------|--------------|---------|-----------|
| Tabela `aura_calibration_events` pode ainda não existir se Story 8.1 não estiver Done — migration falhará com erro de tabela inexistente | Alta | Alto | Confirmar Story 8.1 como Done antes de executar; incluir `IF EXISTS` na migration como guard |
| RLS public para `tipo='prior'` pode expor dados inadvertidamente se policy SELECT for mal escrita (ex: sem filtro adequado) | Baixa | Alto | Testar com usuário não autenticado que a query de priors retorna exatamente as 12 linhas seed e nada mais |

## QA Results
<!-- @qa preenche após implementação -->
```yaml
storyId: 3.1
verdict: PASS
issues: []
```

## Change Log
| Data | Agente | Alteração |
|------|--------|-----------|
| 2026-03-17 | @po (Pax) | Story criada e validada — Status: Draft→Ready pendente |
