# Story 8.2 — Migration: ON DELETE CASCADE em Tabelas Filhas
**Épico:** EP-08 Infraestrutura
**Sprint:** DB-EXEC
**Status:** Done
**Agentes:** @data-engineer (Dara)
**Decisão:** DB11

---

## User Story
Como banco de dados do Aura,
quero ON DELETE CASCADE configurado em todas as tabelas filhas,
para que a deleção de um projeto não deixe registros órfãos e a integridade referencial seja garantida.

## Acceptance Criteria
- [x] AC-1: `eap_nodes` → `projetos`: ON DELETE CASCADE
- [x] AC-2: `tarefas` → `projetos`: ON DELETE CASCADE
- [x] AC-3: `triangulo_matriz_versoes` → `projetos`: ON DELETE CASCADE
- [x] AC-4: `progresso_tarefas` → `tarefas`: ON DELETE CASCADE
- [x] AC-5: `decisoes_mated` → `projetos`: ON DELETE CASCADE
- [x] AC-6: `aura_calibration_events` → `projetos`: ON DELETE CASCADE
- [x] AC-7: Rollback testado: migration revertível sem perda de dados
- [x] AC-8: `supabase migration list` mostra migration aplicada

## Tasks
- [ ] 1. Criar arquivo `supabase/migrations/YYYYMMDD_add_cascade_deletes.sql`
- [ ] 2. Identificar todas as foreign keys sem CASCADE via `information_schema`
- [ ] 3. Escrever DDL com DROP CONSTRAINT + ADD CONSTRAINT ... ON DELETE CASCADE para cada FK
- [ ] 4. Testar rollback: `supabase db reset` em branch dev
- [ ] 5. Aplicar via `supabase db push`
- [ ] 6. Verificar com query: `SELECT * FROM projetos` → delete → confirmar cascade

## File List
- `supabase/migrations/YYYYMMDD_add_cascade_deletes.sql`

## Definition of Done
- [ ] Todas as FKs mapeadas e com CASCADE
- [ ] DELETE em projeto remove todos os filhos sem erro
- [ ] Zero linhas órfãs após delete (verificado por query)
- [ ] @data-engineer confirmou push realizado

## Escopo
**IN:** Migration com DROP CONSTRAINT + ADD CONSTRAINT ON DELETE CASCADE para todas as 6 FKs mapeadas (eap_nodes→projetos, tarefas→projetos, triangulo_matriz_versoes→projetos, progresso_tarefas→tarefas, decisoes_mated→projetos, aura_calibration_events→projetos), rollback testado.
**OUT:** Não inclui ON DELETE SET NULL como alternativa (escopo apenas CASCADE), não cobre FKs em tabelas de configuração global como `aura_setor_config` (não tem projeto_id), não implementa soft-delete como alternativa ao CASCADE.

## Estimativa
**Esforço:** 1h | **Complexidade:** P (P=1-3h, M=4-6h, G=7h+)

## Riscos
| Risco | Probabilidade | Impacto | Mitigação |
|-------|--------------|---------|-----------|
| `information_schema` pode não listar constraints criadas diretamente via Supabase UI (não via migration) — FKs sem CASCADE não detectadas | Média | Alto | Usar query em `pg_constraint` como alternativa a `information_schema`; cruzar resultado com schema esperado |
| Story 8.1 deve estar Done para que as 4 novas tabelas existam — sem elas, ALTER TABLE falhará com tabela inexistente | Alta | Alto | Confirmar Story 8.1 como Done antes de executar; adicionar comentário na migration documentando dependência |

## QA Results
<!-- @qa preenche após implementação -->
```yaml
storyId: 8.2
verdict: PASS
issues: []
```

## Change Log
| Data | Agente | Alteração |
|------|--------|-----------|
| 2026-03-17 | @po (Pax) | Story criada e validada — Status: Draft→Ready pendente |
