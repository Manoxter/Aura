# Story 1.9 — Unificar peAltitude() e projectPointToLine() em triangle-logic.ts
**Épico:** EP-01 Fundamentos Geométricos
**Sprint:** B-FIX
**Status:** Done
**Agentes:** @dev (Dex), @architect (Aria)
**Decisão:** DRY — eliminar lógica duplicada de projeção ortogonal
**Pré-requisito:** —

---

## User Story
Como base de código do Aura,
quero que `peAltitude()` e `projectPointToLine()` compartilhem a mesma implementação matemática,
para eliminar duplicação de lógica de projeção ortogonal e ter um único ponto canônico.

---

## Acceptance Criteria
- [x] AC-1: `projectPointToLine()` exportada de `triangle-logic.ts`
- [x] AC-2: `peAltitude()` em `math.ts` delega para `projectPointToLine()` (sem lógica duplicada)
- [x] AC-3: Todos os callers de `peAltitude()` continuam funcionando sem alteração
- [x] AC-4: TypeCheck: 0 erros novos
- [x] AC-5: 438/438 testes passando (sem regressão)

---

## Tasks
- [x] 1. Exportar `projectPointToLine` de `triangle-logic.ts`
- [x] 2. Importar em `math.ts` e reescrever `peAltitude()` para delegar
- [x] 3. TypeCheck + Vitest

---

## File List
- `src/lib/engine/triangle-logic.ts` — `projectPointToLine` exportada
- `src/lib/engine/math.ts` — `peAltitude()` delegada + import de `projectPointToLine`

---

## Definition of Done
- [x] Lógica duplicada eliminada
- [x] TypeCheck: 0 erros novos
- [x] Vitest: 438/438 passando
- [x] @qa gate: PASS

## Change Log
| Data | Agente | Alteração |
|------|--------|-----------|
| 2026-03-21 | @dev (Dex) | Implementado — export + delegação (438/438) |
