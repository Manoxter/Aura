# Story 13.1 — Migration: tabela riscos_projeto
**Épico:** EP-13 Prometeu Extrínseco
**Sprint:** PROMETEU
**Status:** Done
**Agentes:** @data-engineer (Dara), @security-auditor

---

## User Story
Como engenheiro de banco de dados,
quero criar a tabela `riscos_projeto` com RLS por tenant e índices adequados,
para que o módulo Prometeu possa armazenar e consultar riscos de forma segura e performática.

---

## Acceptance Criteria
- [x] AC-1: Migration cria a tabela `riscos_projeto` com RLS habilitada e política de isolamento por `tenant_id`.
- [x] AC-2: Campo `categoria` aceita apenas os valores: `'escopo'`, `'prazo'`, `'custo'`, `'qualidade'`, `'externo'` (constraint CHECK).
- [x] AC-3: Campos `probabilidade` e `impacto` são do tipo `NUMERIC(5,4)` com constraint `BETWEEN 0 AND 1`.
- [x] AC-4: Campo `score_rc` é calculado pela aplicação (não por trigger de banco) — coluna armazenada sem trigger.
- [x] AC-5: Índice criado em `projeto_id` para performance de consultas por projeto.

---

## Tasks
- [x] 1. Criar migration `20260321500000_create_riscos_projeto.sql` com DDL completo.
- [x] 2. Adicionar constraint CHECK em `categoria` para os 5 valores permitidos.
- [x] 3. Adicionar constraints CHECK em `probabilidade` e `impacto` para range [0, 1].
- [x] 4. Habilitar RLS e criar policy de ALL para `auth.uid()` via `tenant_id`.
- [x] 5. Criar índice `CREATE INDEX ON riscos_projeto(projeto_id)`.
- [ ] 6. (Pendente ambiente) Gerar tipos TypeScript via `supabase gen types` e atualizar `database.types.ts`.

---

## File List
- `supabase/migrations/20260321500000_create_riscos_projeto.sql` — migration DDL

---

## Definition of Done
- [x] TypeCheck: 0 erros novos
- [x] Vitest: N/A (migration SQL)
- [x] @qa gate: PASS

---

## Change Log
| Data | Agente | Alteração |
|------|--------|-----------|
| 2026-03-21 | @sm (River) | Story criada |
| 2026-03-21 | @data-engineer (Dara) | Migration criada — DDL + RLS + índice |
| 2026-03-21 | @qa (Quinn) | @qa gate: PASS |
