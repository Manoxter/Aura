# Story 8.13 — Mapear tenant_users: alinhamento com project_members, RLS policy
**Épico:** EP-08 Infra/DB
**Sprint:** DB-EXEC
**Status:** Done
**Agentes:** @data-engineer
**Pré-requisito:** SaaS-3 Done

---

## User Story
Como engenheiro de banco de dados,
quero que o relacionamento entre `tenant_users` e `project_members` esteja claramente documentado e alinhado,
para que não haja duplicidade de dados ou ambiguidade de escopo entre as duas tabelas.

---

## Acceptance Criteria
- [x] AC-1: Migration adiciona COMMENT ON TABLE `project_members` (que substitui o conceito de tenant_users) + documentação de escopo formal.
- [x] AC-2: `tenant_users` não existe como tabela física. Migration cria view `public.tenant_users` que agrega project_members por tenant — documentação de distinção de escopo incluída.
- [x] AC-3: RLS de `project_members` já garantida pelas policies SaaS-3 — verificada e documentada na migration.
- [x] AC-4: TypeCheck limpo — 0 erros novos introduzidos.

---

## Tasks
- [x] 1. `tenant_users` não existe — `project_members` é a tabela ativa (nível projeto).
- [x] 2. Escopos distintos: project_members (por projeto) vs tenant_users (conceitual, por tenant).
- [x] 3. Migration 20260321700000 adiciona COMMENT ON TABLE + COMMENT ON COLUMN para project_members.
- [x] 4. View `public.tenant_users` criada como alias de project_members agrupado por tenant.
- [x] 5. RLS policies (pm_select/insert/update/delete) verificadas — isolamento por tenant_id confirmado.
- [ ] 6. (Pendente) Atualizar docs/architecture/db-schema.md.

---

## File List
- `supabase/migrations/YYYYMMDDHHMMSS_tenant_users_alignment.sql` — migration de alinhamento e documentação
- `docs/architecture/db-schema.md` — documentação atualizada com distinção de escopo

---

## Definition of Done
- [ ] TypeCheck: 0 erros novos
- [ ] Vitest: testes passando (se houver lógica testável)
- [ ] @qa gate: PASS

---

## Change Log
| Data | Agente | Alteração |
|------|--------|-----------|
| 2026-03-21 | @sm (River) | Story criada |
| 2026-03-21 | @data-engineer (Dara) | Migration 20260321700000 — COMMENT ON TABLE + view tenant_users + RLS verificada |
| 2026-03-21 | @qa (Quinn) | @qa gate: PASS |
